# _forAI Guide

## 목차

- [한 줄 요약](#한-줄-요약)
- [읽는 순서](#읽는-순서)
- [문서 역할](#문서-역할)
- [현재 스냅샷](#현재-스냅샷)
- [유지 규칙](#유지-규칙)

## 한 줄 요약

이 디렉터리는 `ready_chromedev` 작업을 이어받을 때 필요한 AI 작업 문맥을 정리해 두는 곳이다.
주제는 **"AI를 브라우저에 붙이는 방법"** 이고, 방법이 여러 갈래라는 것이 이 저장소의 핵심 발견이다.

## 읽는 순서

1. `README.md`
2. `inventory.md`
3. `memo.md`  ← **방법론 비교표가 여기 있다. 제일 중요하다.**
4. `dev_log.md`
5. `plan.md`

## 문서 역할

- `inventory.md`: 저장소에 실제로 있는 구조, 엔트리포인트, 빌드/검증 명령을 기록한다.
- `plan.md`: 앞으로 진행할 개발 계획과 우선순위만 기록한다.
- `memo.md`: 프로토콜, 방법론 비교, 기본값, 디버깅 교훈 같은 참고 메모를 모은다.
- `dev_log.md`: 날짜별 작업 이력과 `_forAI` 정리 내역을 남긴다.

## 현재 스냅샷

- 저장소 경로: `c:\works\ready_chromedev`
- git 저장소: **아님** (`.gitignore` 만 있고 `.git` 없음)
- 플랫폼: Windows 11 Home 26200 / Node v24.18.0 (nvm-windows) / Chrome 150.0.7871.115
- 메인 엔트리포인트: `demo/server.mjs` (의존성 0, 포트 8787)
- 검증 도구: `demo/tools/*.mjs` — 전부 MCP 서버를 직접 spawn 해 JSON-RPC 로 대화한다
- 목적: Chrome MCP 특강 준비 + MCP 개념 학습 + 후속 실험

## 유지 규칙

- 계획이 아닌 참고 정보는 `plan.md`가 아니라 `memo.md`에 둔다.
- 저장소 구조나 실행 명령이 바뀌면 `inventory.md`를 먼저 갱신한다.
- 작업 이력은 날짜를 붙여 `dev_log.md`에만 남긴다.
- 새 작업을 시작할 때는 `inventory.md`와 `memo.md`를 먼저 읽고, 실제 할 일은 `plan.md`에서 확인한다.
- 모든 문서에는 제목 바로 아래에 `## 목차` 섹션을 둔다.
- **주장에는 근거를 붙인다.** 이 저장소의 사실은 웹 검색이 아니라 실행해서 얻었다.
  검증 안 한 것은 "미검증"이라고 명시한다.
- 사용자 동의 없이 git commit을 하지 않는다.
- 사용자 동의 없이 `_forAI/` 문서를 수정하지 않는다.
