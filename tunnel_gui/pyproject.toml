[project]
name = "chrome-devtools-tunnel-gui"
version = "0.2.0"
description = "Python Tkinter manager for Chrome DevTools SSH reverse tunnels"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
